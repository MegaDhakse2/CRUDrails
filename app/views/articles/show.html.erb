<%= @article.title%>
<%= @article.body%> <br>

<%= link_to 'edit', edit_article_path %>
<br>
<%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>

